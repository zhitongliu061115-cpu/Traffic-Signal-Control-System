server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    com.traffic.agent: DEBUG

spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - optional:file:backend/.env[.properties]
  application:
    name: traffic-signal-backend
  profiles:
    group:
      lan:
        - postgres
        - lan-server
  datasource:
    url: jdbc:h2:mem:traffic_signal;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: ${QQ_MAIL_HOST:smtp.qq.com}
    port: ${QQ_MAIL_PORT:465}
    username: ${QQ_MAIL_USERNAME:}
    password: ${QQ_MAIL_AUTH_CODE:}
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            fallback: false

auth:
  mail:
    from: ${QQ_MAIL_FROM:${QQ_MAIL_USERNAME:}}
    sender-name: ${QQ_MAIL_SENDER_NAME:信号灯配时控制系统}
  captcha:
    ttl-minutes: ${AUTH_CAPTCHA_TTL_MINUTES:5}
    resend-interval-seconds: ${AUTH_CAPTCHA_RESEND_INTERVAL_SECONDS:60}
  invite-code: ${AUTH_INVITE_CODE:123456}
  initial-account:
    username: ${AUTH_INITIAL_USERNAME:admin}
    password: ${AUTH_INITIAL_PASSWORD:123456}
    email: ${AUTH_INITIAL_EMAIL:admin@traffic.local}

cityflow:
  base-url: ${CITYFLOW_BASE_URL:http://39.105.75.87:9000}
  api-token: ${CITYFLOW_API_TOKEN:jLEc-o3L16migUKQ7f_OlH94qsjEstFf}
  client-id: ${CITYFLOW_CLIENT_ID:hcj}
  default-scene-id: jinan_3x4
  frame-poll-interval-ms: 100

traffic:
  telemetry:
    sample-interval-ms: ${TRAFFIC_TELEMETRY_SAMPLE_INTERVAL_MS:1000}
  agent:
    langchain4j:
      enabled: ${AGENT_LANGCHAIN4J_ENABLED:false}
      base-url: ${DASHSCOPE_COMPATIBLE_BASE_URL:${BAILIAN_COMPATIBLE_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode/v1}}
      api-key: ${DASHSCOPE_API_KEY:${BAILIAN_API_KEY:}}
      model-name: ${DASHSCOPE_MODEL:${BAILIAN_MODEL:qwen-plus}}
      temperature: ${AGENT_MODEL_TEMPERATURE:0.2}
      timeout-seconds: ${AGENT_MODEL_TIMEOUT_SECONDS:60}

traffic-r:
  enabled: ${TRAFFIC_R_ENABLED:true}
  base-url: ${TRAFFIC_R_BASE_URL:http://127.0.0.1:16008}
  health-path: ${TRAFFIC_R_HEALTH_PATH:/health}
  predict-path: ${TRAFFIC_R_PREDICT_PATH:/predict}
  batch-predict-path: ${TRAFFIC_R_BATCH_PREDICT_PATH:/predict-batch}
  decision-interval-sec: ${TRAFFIC_R_DECISION_INTERVAL_SEC:10}
  timeout-sec: ${TRAFFIC_R_TIMEOUT_SEC:30}
  fallback-controller: ${TRAFFIC_R_FALLBACK_CONTROLLER:max-pressure}

bailian:
  base-url: ${DASHSCOPE_HTTP_BASE_URL:${BAILIAN_BASE_URL:https://dashscope.aliyuncs.com/api/v1}}
  app-id: ${DASHSCOPE_APP_ID:${BAILIAN_APP_ID:d77d3dffe51447ecb6d3d34e7a779560}}
  api-key: ${DASHSCOPE_API_KEY:${BAILIAN_API_KEY:}}
  call-mode: ${BAILIAN_CALL_MODE:app}
  compatible-base-url: ${DASHSCOPE_COMPATIBLE_BASE_URL:${BAILIAN_COMPATIBLE_BASE_URL:}}
  model: ${DASHSCOPE_MODEL:${BAILIAN_MODEL:qwen-plus}}
  connect-timeout-ms: ${BAILIAN_CONNECT_TIMEOUT_MS:10000}
  read-timeout-ms: ${BAILIAN_READ_TIMEOUT_MS:30000}

---

spring:
  config:
    activate:
      on-profile: lan-server

server:
  address: ${SERVER_ADDRESS:0.0.0.0}

---

spring:
  config:
    activate:
      on-profile: postgres
  datasource:
    url: jdbc:postgresql://120.48.99.240:5432/traffic_signal
    username: traffic_user
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: ${TRAFFIC_DB_FLYWAY_ENABLED:true}
